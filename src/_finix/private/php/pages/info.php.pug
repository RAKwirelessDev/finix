extends ./page.tpl.pug

block init
    - var title = `finix info page - v${pkg.version}`
    - var module = 'info'

block body
    div#content
        h1#header
            a(href=pkg.homepage) finix
